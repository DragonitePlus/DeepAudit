# Database Configuration (MySQL + P6Spy)
spring.datasource.url=jdbc:p6spy:mysql://localhost:3306/deepaudit?serverTimezone=Asia/Shanghai&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=1740084968
spring.datasource.driver-class-name=com.p6spy.engine.spy.P6SpyDriver
spring.datasource.type=com.alibaba.druid.pool.DruidDataSource

mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl
mybatis-plus.mapper-locations=classpath:mapper/*.xml
mybatis-plus.type-aliases-package=edu.hnu.deepaudit.model

# Initialize Schema (Ensure tables exist)
spring.sql.init.mode=always
spring.sql.init.encoding=UTF-8

# Test Filtering Config
deepaudit.filter.excluded-tables=sys_audit_log
deepaudit.filter.system-tables=sys_risk_rule

# Redis Configuration
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.database=0
spring.data.redis.timeout=60000
