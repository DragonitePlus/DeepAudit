# ===================================================================
# Standard Single DataSource Configuration (Only sys DB)
# ===================================================================
spring.datasource.url=jdbc:mysql://localhost:3306/deepaudit_sys?serverTimezone=Asia/Shanghai&characterEncoding=utf8&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=1740084968
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection Pool Settings (HikariCP)
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.pool-name=DeepAuditSysPool

# MyBatis Plus Configuration
mybatis-plus.mapper-locations=classpath*:mapper/**/*.xml
mybatis-plus.type-aliases-package=edu.hnu.deepaudit.model

# Initialize Schema (Handled by custom config or tests, disabling default)
spring.sql.init.mode=never
spring.sql.init.encoding=UTF-8

# Redis Configuration
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.database=0
spring.data.redis.timeout=60000
